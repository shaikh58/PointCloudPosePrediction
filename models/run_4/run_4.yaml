keypoint_detection:
  detection_method : sift
  pcd_num_pts : 2000 # note, sift only reliably generates ~2000 key points
model:
    use_pca : True
train:
    epochs : 10
    lr : 0.001
    batch_size : 20
    